each checkin in checkins.reverse()
  .checkin
    .mainline
      strong.user= checkin.user || 'Anonymous'
      if checkin.roastery || checkin.coffee
        |  is drinking a 
        if checkin.roastery && checkin.coffee
          strong.roastery= checkin.roastery
          |  
        strong.coffee= checkin.coffee || checkin.roastery
        if checkin.brew_method
          |  
          strong.brew_method= checkin.brew_method
      else
        |  is having a  
        strong.brew_method= checkin.brew_method
      if checkin.location
        |  at 
        strong.location= checkin.location
      | .
    .details
      if checkin.origin
        span
          | Origin:
          strong.origin= checkin.origin
    if checkin.comment
      .comment= checkin.comment
    .time= checkin.pretty_age

